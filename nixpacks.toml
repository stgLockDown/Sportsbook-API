# Railway Nixpacks configuration (alternative to Dockerfile)

[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python main.py"

[variables]
PYTHON_VERSION = "3.11"